# SkipList
an implementation for skipList using scala
1. add (key, value)      log(n)
2. delete(key)           log(n)
3. find(index)           log(n)
4. find(key)             log(n)
5. min/max               O(1)
